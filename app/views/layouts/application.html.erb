<!DOCTYPE html>
<html lang="en">
  <head>
    <title><%= content_for(:title) || "Qlarity" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="application-name" content="Qlarity">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-gradient-to-br from-slate-50 via-white to-indigo-50/30 min-h-screen">
    <%# Skip to main content for keyboard/screen-reader users (WCAG 2.4.1) %>
    <a href="#main-content" class="sr-only focus:not-sr-only focus:absolute focus:top-2 focus:left-2 focus:z-[60] focus:bg-white focus:px-4 focus:py-2 focus:rounded-lg focus:shadow-lg focus:text-indigo-600 focus:font-semibold">
      Skip to main content
    </a>

    <%# Navigation bar %>
    <nav class="bg-white/80 backdrop-blur-md shadow-sm border-b border-gray-200/50 fixed top-0 left-0 right-0 z-50" role="navigation" aria-label="Main navigation">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="flex items-center justify-between h-16">
          <%# Logo %>
          <%= link_to root_path, class: "flex items-center gap-2 group", "aria-label": "Qlarity home" do %>
            <svg width="32" height="32" viewBox="0 0 512 512" class="flex-shrink-0" aria-hidden="true">
              <defs>
                <linearGradient id="nav-logo-bg" x1="0%" y1="0%" x2="100%" y2="100%">
                  <stop offset="0%" stop-color="#6366f1"/>
                  <stop offset="100%" stop-color="#a855f7"/>
                </linearGradient>
              </defs>
              <rect width="512" height="512" rx="96" fill="url(#nav-logo-bg)"/>
              <text x="256" y="340" text-anchor="middle" font-family="system-ui, sans-serif" font-size="320" font-weight="bold" fill="white">Q</text>
            </svg>
            <span class="text-2xl font-bold text-gradient group-hover:opacity-80 transition-opacity">Qlarity</span>
          <% end %>

          <%# Nav links %>
          <div class="flex items-center gap-1">
            <%= link_to "Upload", upload_path,
                  class: "px-4 py-2 text-sm font-medium rounded-lg transition-colors focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:ring-offset-2 #{request.path == '/upload' ? 'text-indigo-600 bg-indigo-50' : 'text-gray-700 hover:text-indigo-600 hover:bg-gray-50'}" %>
            <%= link_to "Profile", profile_path,
                  class: "px-4 py-2 text-sm font-medium rounded-lg transition-colors focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:ring-offset-2 #{request.path == '/profile' ? 'text-indigo-600 bg-indigo-50' : 'text-gray-700 hover:text-indigo-600 hover:bg-gray-50'}" %>
          </div>
        </div>
      </div>
    </nav>

    <%# Flash messages %>
    <% if flash[:notice] || flash[:alert] %>
      <div class="max-w-4xl mx-auto pt-20 px-4 space-y-2">
        <% if flash[:notice] %>
          <div class="bg-green-50 border border-green-200 text-green-700 px-4 py-3 rounded-lg" role="status">
            <%= flash[:notice] %>
          </div>
        <% end %>
        <% if flash[:alert] %>
          <div class="bg-red-50 border border-red-200 text-red-700 px-4 py-3 rounded-lg" role="alert">
            <%= flash[:alert] %>
          </div>
        <% end %>
      </div>
    <% end %>

    <main id="main-content" class="pt-20 pb-10">
      <%= yield %>
    </main>
  </body>
</html>
