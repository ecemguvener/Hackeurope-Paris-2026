<!DOCTYPE html>
<html lang="en">
  <head>
    <title><%= content_for(:title) || "Qlarity" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="application-name" content="Qlarity">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%# FOUC prevention: apply theme on <html> before first paint (body classes handled by Stimulus) %>
    <script>
      (function() {
        try {
          var h = document.documentElement;
          var t = localStorage.getItem('qlarity-theme');
          var c = localStorage.getItem('qlarity-contrast');
          var o = localStorage.getItem('qlarity-overlay');
          if (t) h.setAttribute('data-theme', t);
          if (c) h.setAttribute('data-contrast', c);
          if (o) h.setAttribute('data-overlay', o);
        } catch(e) {}
      })();
    </script>

    <%= yield :head %>

    <%# Load OpenDyslexic font %>
    <link rel="preconnect" href="https://fonts.cdnfonts.com">
    <link rel="stylesheet" href="https://fonts.cdnfonts.com/css/open-dyslexic">

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="min-h-screen" style="background-color: var(--color-bg, #f8f7f4); color: var(--color-text, #1e1b4b);"
        data-controller="accessibility">
    <%# Skip to main content for keyboard/screen-reader users (WCAG 2.4.1) %>
    <a href="#main-content" class="sr-only focus:not-sr-only focus:absolute focus:top-2 focus:left-2 focus:z-[60] focus:bg-white focus:px-4 focus:py-2 focus:rounded-lg focus:shadow-lg focus:text-indigo-600 focus:font-semibold">
      Skip to main content
    </a>

    <%# Navigation bar %>
    <nav class="shadow-sm border-b fixed top-0 left-0 right-0 z-50" role="navigation" aria-label="Main navigation"
         style="background-color: var(--color-nav-bg); border-color: var(--color-nav-border); backdrop-filter: blur(12px);">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="flex items-center justify-between h-16">
          <%# Logo %>
          <%= link_to root_path, class: "flex items-center gap-2 group", "aria-label": "Qlarity home" do %>
            <svg width="32" height="32" viewBox="0 0 512 512" style="width:32px;height:32px" aria-hidden="true">
              <defs>
                <linearGradient id="nav-logo-bg" x1="0%" y1="0%" x2="100%" y2="100%">
                  <stop offset="0%" stop-color="#6366f1"/>
                  <stop offset="100%" stop-color="#a855f7"/>
                </linearGradient>
              </defs>
              <rect width="512" height="512" rx="96" fill="url(#nav-logo-bg)"/>
              <text x="256" y="340" text-anchor="middle" font-family="system-ui, sans-serif" font-size="320" font-weight="bold" fill="white">Q</text>
            </svg>
            <span class="text-2xl font-bold text-gradient group-hover:opacity-80 transition-opacity">Qlarity</span>
          <% end %>

          <%# Nav links + Accessibility toolbar trigger %>
          <div class="flex items-center gap-1">
            <%= link_to "Upload", upload_path,
                  class: "px-4 py-2 text-sm font-medium rounded-lg transition-colors focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:ring-offset-2 #{request.path == '/upload' ? 'text-indigo-600 bg-indigo-50' : 'text-gray-900 hover:text-indigo-600 hover:bg-gray-50'}" %>
            <%= link_to "Profile", profile_path,
                  class: "px-4 py-2 text-sm font-medium rounded-lg transition-colors focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:ring-offset-2 #{request.path == '/profile' ? 'text-indigo-600 bg-indigo-50' : 'text-gray-900 hover:text-indigo-600 hover:bg-gray-50'}" %>
            <%= link_to "Billing", billing_path,
                  class: "px-4 py-2 text-sm font-medium rounded-lg transition-colors focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:ring-offset-2 #{request.path == '/billing' ? 'text-indigo-600 bg-indigo-50' : 'text-gray-900 hover:text-indigo-600 hover:bg-gray-50'}" %>

            <%# Accessibility toolbar trigger %>
            <button data-action="accessibility#togglePanel"
                    class="ml-2 px-3 py-2 text-sm font-bold rounded-lg border-2 border-indigo-300 text-indigo-600 hover:bg-indigo-50 transition-colors focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:ring-offset-2"
                    aria-label="Accessibility settings"
                    title="Accessibility settings">
              Aa
            </button>
          </div>
        </div>
      </div>

      <%# Accessibility toolbar panel %>
      <%= render "shared/accessibility_toolbar" %>
    </nav>

    <%# Flash messages %>
    <% if flash[:notice] || flash[:alert] %>
      <div class="max-w-4xl mx-auto pt-20 px-4 space-y-2">
        <% if flash[:notice] %>
          <div class="bg-green-50 border border-green-200 text-green-700 px-4 py-3 rounded-lg" role="status">
            <%= flash[:notice] %>
          </div>
        <% end %>
        <% if flash[:alert] %>
          <div class="bg-red-50 border border-red-200 text-red-700 px-4 py-3 rounded-lg" role="alert">
            <%= flash[:alert] %>
          </div>
        <% end %>
      </div>
    <% end %>

    <main id="main-content" class="pt-20 pb-10">
      <%= yield %>
    </main>
  </body>
</html>
